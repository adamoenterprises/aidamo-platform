<svg width="512" height="512" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="512" height="512" rx="96" fill="#171717"/>

  <!-- Outer Triangle (A shape) -->
  <path
    d="M256 80 L420 400 L92 400 Z"
    fill="none"
    stroke="white"
    stroke-width="24"
    stroke-linejoin="round"
  />

  <!-- Inner horizontal bar (A crossbar) -->
  <line
    x1="148"
    y1="320"
    x2="364"
    y2="320"
    stroke="white"
    stroke-width="24"
    stroke-linecap="round"
  />

  <!-- Small inner triangle (futuristic element) -->
  <path
    d="M256 160 L320 280 L192 280 Z"
    fill="white"
    opacity="0.3"
  />
</svg>
